<UserControl x:Class="PDC_System.Jobs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PDC_System"
             mc:Ignorable="d"
              xmlns:cal="clr-namespace:ModernCalendarLib;assembly=ModernCalendarLib"
             MinWidth="1004" MinHeight="660">

    <UserControl.Resources>
        <!-- Add the converter here -->
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>


    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

   


        <StackPanel Grid.Row="0">
            
        </StackPanel>

       
            <StackPanel Grid.Row="1">
                <TextBox Margin="0,10,0,10" HorizontalAlignment="Left" Grid.Row="0" Name="NameAutoCompleteBox1" ToolTip="Search by Employee Name or ID" Height="35" Style="{DynamicResource OutsourceSerchBar}" Width="300" TextChanged="SearchBox_TextChanged2" />
            </StackPanel>
            
            <StackPanel Grid.Row="2" Orientation="Horizontal">

                <Label Content="From:" VerticalAlignment="Center" Foreground="White"/>

                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="2" Height="35" CornerRadius="5">
                    <cal:CustomDatePicker x:Name="StartDatePicker" Width="250"/>
                </Border>
                <Label Content="To:" VerticalAlignment="Center" Foreground="White"/>
                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="2" Height="35" CornerRadius="5">
                    <cal:CustomDatePicker x:Name="EndDatePicker" Width="250"/>
                </Border>




                <Button Content="Search" Click="Search_Click" Margin="5" Width="150" Height="35"  Style="{StaticResource ActionButton}" />
                <Button Content="Reset" Click="Reset_Click" Margin="5" Width="80" Height="35"  Style="{StaticResource NavigationButtonStyle}" />

            </StackPanel>



       


        <StackPanel Grid.Row="3">
            <Border Background="Transparent" CornerRadius="30" Height="483" >
                <StackPanel Margin="10" VerticalAlignment="Top">






                    <Button Content="+ Add Job" Click="AddJobCard_Click" HorizontalAlignment="Right" Margin="10" Width="150"  Style="{StaticResource ActionButton}" Height="30"/>

                    <DataGrid x:Name="JobCardDataGrid"

Background="Transparent"
RowBackground="White"
AlternatingRowBackground="#f0f0f0"
SnapsToDevicePixels="True"
AutoGenerateColumns="False"
ItemsSource="{Binding UpcomingBirthdays}"
CanUserResizeColumns="False"
ScrollViewer.CanContentScroll="True"
ScrollViewer.VerticalScrollBarVisibility="Hidden"
Height="500"
CellStyle="{StaticResource DataGridCellStyle}"
Style="{StaticResource DataGridStyle}"
ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
RowStyle="{StaticResource DataGridRowStyle}">
                        
                        <DataGrid.Columns>
                            <!-- Action/Edit button -->
                            <DataGridTemplateColumn Header="Select" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox  IsChecked="{Binding IsSeen, UpdateSourceTrigger=PropertyChanged}"
Visibility="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, 
                   Path=IsSelected, 
                   Converter={StaticResource BooleanToVisibilityConverter}}"
LostFocus="CheckBox_LostFocus" />


                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Date And Time" Binding="{Binding JobCardDate}" Width="*" ElementStyle="{DynamicResource DataGridColumnsStyle}" />
                            <DataGridTextColumn Header="Customer Name" Binding="{Binding Customer_Name}" Width="*" ElementStyle="{DynamicResource DataGridColumnsStyle}" />
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" ElementStyle="{DynamicResource DataGridColumnsStyle}" />
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*" ElementStyle="{DynamicResource DataGridColumnsStyle}" />

                            <DataGridTemplateColumn Header="Action" Width="auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Margin="3" Content="View"  Style="{DynamicResource ActionButton}" Click="OpenJobCardButton_Click"/>
                                            <Button Margin="3" Content="Delete" Style="{DynamicResource NavigationButtonStyle}" Click="DeleteJob_Click"/>
                                        </StackPanel>



                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>


                        </DataGrid.Columns>
                    </DataGrid>








           
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
