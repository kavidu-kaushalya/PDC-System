<UserControl x:Class="PDC_System.UserManagerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PDC_System"
             FontFamily="{StaticResource CustomFont}"
             Foreground="{DynamicResource Forground}"
             >

    <UserControl.Resources>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
    </UserControl.Resources>

    <Border Padding="30"
            CornerRadius="15"
            Background="{DynamicResource Background}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <StackPanel Grid.Row="0" Margin="0,0,0,30">
                <TextBlock Text="User Management System"
                           FontSize="25
                           "
                           FontWeight="Bold"
                           Foreground="{DynamicResource Forground}"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Manage user accounts and permissions for your PDC system"
                           FontSize="13
                           "
                           Opacity="0.7"
                           Foreground="{DynamicResource Forground}"/>
            </StackPanel>

            <!-- Navigation Tabs -->
            <Border Grid.Row="1" 
                    Background="{DynamicResource ThemeBackground}"
                    CornerRadius="10"
                    Padding="5"
                    Margin="0,0,0,25">
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="BtnCreateUser" 
                            Content="➕ Create User"
                          
                            Width="180" 
                            Margin="5"
                           
                          
                            Click="BtnCreateUser_Click"
                            Style="{DynamicResource ActionButton}" 
                            />


                </StackPanel>
            </Border>

            <!-- Main Content Area -->
            <Grid Grid.Row="2" HorizontalAlignment="Stretch" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>

                </Grid.ColumnDefinitions>

                <!-- Users Management Panel (Left) -->
                <Border Grid.Column="0" 
                        x:Name="ManageUsersPanel" 
                        Visibility="Visible"
                        Background="{DynamicResource Background}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="25">

                    <StackPanel>
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Existing Users"
                                           FontSize="20"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource Forground}"
                                           Margin="0,0,0,5"/>
                                <TextBlock Text="View and manage system user accounts"
                                           FontSize="14"
                                           Opacity="0.7"
                                           Foreground="{DynamicResource Forground}"/>
                            </StackPanel>

                            <Border Grid.Column="1"
                                    Background="{DynamicResource ThemeBackground}"
                                    CornerRadius="8"
                                    Padding="10,8">
                                <TextBlock x:Name="UserCountText"
                                           Text="0 Users"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource Forground}"/>
                            </Border>
                        </Grid>

                        <!-- Enhanced DataGrid -->
                        <Border Background="{DynamicResource Background}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="5">

                            <DataGrid x:Name="UsersDataGrid"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      GridLinesVisibility="None"
                                      HeadersVisibility="Column"
                                      SelectionMode="Single"
                                      RowHeaderWidth="0"
                                      FontSize="13"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      MinHeight="400"
                                      
                                        CellStyle="{StaticResource DataGridCellStyle}"
  Style="{StaticResource DataGridStyle}"
  ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
  RowStyle="{StaticResource DataGridRowStyle}"
                                      
                                      MaxHeight="500">


                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Username" 
                                                     
                                                        Binding="{Binding Username}" 
                                                        Width="150"
                                                         ElementStyle="{DynamicResource DataGridColumnsStyle}"/>


                                    <!-- Dashboard -->
                                    <DataGridTemplateColumn Header="Dashboard" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsEnabled="False" IsChecked="{Binding Dashbord}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- Order Check -->
                                    <DataGridTemplateColumn Header="Order Check" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsEnabled="False" IsChecked="{Binding OderCheck}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- Job Card -->
                                    <DataGridTemplateColumn Header="Job Card" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsEnabled="False" IsChecked="{Binding Jobcard}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- Customer -->
                                    <DataGridTemplateColumn Header="Customer" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsEnabled="False" IsChecked="{Binding Customer}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- Employee -->
                                    <DataGridTemplateColumn Header="Employee" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsEnabled="False" IsChecked="{Binding Employee}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- Admin -->
                                    <DataGridTemplateColumn Header="Admin" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsEnabled="False" IsChecked="{Binding Isadmin}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>


                                    <DataGridTemplateColumn Header="Actions" Width="140">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" 
                                                            HorizontalAlignment="Center"
                                                            Visibility="{Binding Isadmin, Converter={StaticResource InverseBoolToVis}}">
                                                    <Button Content="✏️"
                                                            ToolTip="Edit User"
                                                            Width="32" 
                                                            Height="32"
                                                            Margin="2,0"
                                                            Click="EditUser_Click"
                                                            Style="{DynamicResource EditActionButton}"
                                                           />

                                                    <Button Content="🗑️"
                                                            ToolTip="Delete User"
                                                            Width="32" 
                                                            Height="32"
                                                            Margin="2,0"
                                                            Click="DeleteUser_Click"
                                                            Style="{DynamicResource DeleteActionButton}"
                                                            />
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Create/Edit User Panel (Right) -->
                <Border Grid.Column="1" 
        x:Name="CreateUserPanel" 
        Visibility="Collapsed"
        Background="{DynamicResource Background}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="1"
        CornerRadius="12"
        Padding="25" Margin="10,0,0,0">

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- Panel Header -->
                            <Border Background="{DynamicResource ThemeBackground}"
                    CornerRadius="8"
                    Padding="15"
                    Margin="0,0,0,25">
                                <StackPanel>
                                    <TextBlock Text="➕ Create New User"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="{DynamicResource Forground}"
                               Margin="0,0,0,5"/>
                                    <TextBlock Text="Fill in the details to create a new user account"
                               FontSize="12"
                               Opacity="0.7"
                               Foreground="{DynamicResource Forground}"/>
                                </StackPanel>
                            </Border>

                            <!-- User Details Section -->
                            <Border Background="{DynamicResource Background}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20"
                    Margin="0,0,0,20">

                                <StackPanel>
                                    <TextBlock Text="👤 User Details"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource Forground}"
                               Margin="0,0,0,15"/>

                                    <!-- Username -->
                                    <StackPanel Margin="0,0,0,15">
                                        <TextBlock Text="Username"
                                   FontSize="13"
                                   FontWeight="Medium"
                                   Foreground="{DynamicResource Forground}"
                                   Margin="0,0,0,5"/>
                                        <TextBox x:Name="TxtUser"
                                 Style="{DynamicResource TextBoxStyle}"
                                 Height="40"
                                 FontSize="14"/>
                                    </StackPanel>

                                    <!-- Password -->
                                    <StackPanel Margin="0,0,0,15">
                                        <TextBlock Text="Password"
                                   FontSize="13"
                                   FontWeight="Medium"
                                   Foreground="{DynamicResource Forground}"
                                   Margin="0,0,0,5"/>
                                        <PasswordBox x:Name="TxtPass"
                                     Height="40"
                                     FontSize="14"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Permissions Section -->
                            <Border Background="{DynamicResource Background}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20"
                    Margin="0,0,0,20">

                                <StackPanel>
                                    <TextBlock Text="🔐 Permissions Access"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource Forground}"
                               Margin="0,0,0,15"/>

                                    <!-- Basic Permissions -->
                                    <TextBlock Text="Basic Access"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="{DynamicResource Forground}"
                               Opacity="0.8"
                               Margin="0,0,0,10"/>

                                    <Border 
                            CornerRadius="6"
                            Padding="15"
                            Margin="0,0,0,15">

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="250"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Dashboard (Disabled) -->
                                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="🏠 Dashboard"
                                       Foreground="{DynamicResource Forground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                                            <CheckBox Grid.Row="0" Grid.Column="1"
                                      x:Name="Dashbord"
                                      IsChecked="True"
                                      IsEnabled="False"
                                      VerticalAlignment="Center"
                                      Margin="0,8"/>

                                            <!-- Order Check -->
                                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="📋 Order Check"
                                       Foreground="{DynamicResource Forground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                                            <CheckBox Grid.Row="1" Grid.Column="1"
                                      x:Name="ChkOderCheck"
                                      VerticalAlignment="Center"
                                      Margin="0,8"/>

                                            <!-- Job Card -->
                                            <TextBlock Grid.Row="2" Grid.Column="0"
                                       Text="🎯 Job Card"
                                       Foreground="{DynamicResource Forground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                                            <CheckBox Grid.Row="2" Grid.Column="1"
                                      x:Name="ChkJobcard"
                                      VerticalAlignment="Center"
                                      Margin="0,8"/>

                                            <!-- Customer Management -->
                                            <TextBlock Grid.Row="3" Grid.Column="0"
                                       Text="👥 Customer Management"
                                       Foreground="{DynamicResource Forground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                                            <CheckBox Grid.Row="3" Grid.Column="1"
                                      x:Name="ChkCustomer"
                                      VerticalAlignment="Center"
                                      Margin="0,8"/>

                                            <!-- Quotation -->
                                            <TextBlock Grid.Row="4" Grid.Column="0"
                                       Text="💰 Quotation"
                                       Foreground="{DynamicResource Forground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                                            <CheckBox Grid.Row="4" Grid.Column="1"
                                      x:Name="ChkQuotation"
                                      VerticalAlignment="Center"
                                      Margin="0,8"/>
                                        </Grid>

                                    </Border>

                                    <!-- Advanced Permissions -->
                                    <TextBlock Text="Advanced Access"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="{DynamicResource Forground}"
                               Opacity="0.8"
                               Margin="0,10,0,10"/>

                                    <Border Background="{DynamicResource ThemeBackground}"
                            CornerRadius="6"
                            Padding="15"
                            Margin="0,0,0,15">

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="250"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Outsourcing -->
                                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="🏢 Outsourcing"
                                       Foreground="{DynamicResource Forground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                                            <CheckBox Grid.Row="0" Grid.Column="1"
                                      x:Name="ChkOutsourcing"
                                      VerticalAlignment="Center"
                                      Margin="0,8"/>

                                            <!-- Employee Management -->
                                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="👤 Employee Management"
                                       Foreground="{DynamicResource Forground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                                            <CheckBox Grid.Row="1" Grid.Column="1"
                                      x:Name="ChkEmployee"
                                      VerticalAlignment="Center"
                                      Margin="0,8"/>

                                            <!-- Attendance -->
                                            <TextBlock Grid.Row="2" Grid.Column="0"
                                       Text="📅 Attendance"
                                       Foreground="{DynamicResource Forground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                                            <CheckBox Grid.Row="2" Grid.Column="1"
                                      x:Name="ChkAttendance"
                                      VerticalAlignment="Center"
                                      Margin="0,8"/>

                                            <!-- Payroll Management -->
                                            <TextBlock Grid.Row="3" Grid.Column="0"
                                       Text="💳 Payroll Management"
                                       Foreground="{DynamicResource Forground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                                            <CheckBox Grid.Row="3" Grid.Column="1"
                                      x:Name="ChkPayroll"
                                      VerticalAlignment="Center"
                                      Margin="0,8"/>

                                            <!-- Paysheet -->
                                            <TextBlock Grid.Row="4" Grid.Column="0"
                                       Text="📊 Paysheet"
                                       Foreground="{DynamicResource Forground}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                                            <CheckBox Grid.Row="4" Grid.Column="1"
                                      x:Name="ChkPaysheet"
                                      VerticalAlignment="Center"
                                      Margin="0,8"/>

                                            <!-- Admin (Highlighted) -->
                                            <Border Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                                    Background="{DynamicResource Background}"
                                    CornerRadius="6"
                                    Padding="8"
                                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="2"
                                    Margin="0,8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="235"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0"
                                               Text="🔧 System Administrator"
                                               Foreground="White"
                                               FontWeight="SemiBold"
                                               FontSize="13"
                                               VerticalAlignment="Center"/>
                                                    <CheckBox Grid.Column="1"
                                              x:Name="ChkIsAdmin"
                                              VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>

                                        </Grid>

                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- Action Buttons -->
                            <Border Background="{DynamicResource ThemeBackground}"
                    CornerRadius="8"
                    Padding="15">

                                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center">
                                    <Button x:Name="BtnCreateSave" 
                            Content="✅ Create User"
                            Width="140" 
                            
                            Margin="0,0,15,0"
                           
                           
                            Click="CreateSave_Click"
                            Style="{DynamicResource ActionButton}"
                           />

                                    <Button x:Name="BtnClear" 
                            Content="❌ Cancel"
                            Width="100"
                            
                            
                            Click="Clear_Click"
                            Style="{DynamicResource DeleteActionButton}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>