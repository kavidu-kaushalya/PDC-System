<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <SolidColorBrush x:Key="BtnSignLoadingColor" Color="White"/>
    <SolidColorBrush x:Key="BtnSignLoadingFColor" Color="White"/>

    <Style x:Key="RunningProgressBarStyle" TargetType="{x:Type ProgressBar}">
        <Setter Property="Foreground" Value="#FF0078D7"/>
        <Setter Property="Background" Value="#9ED2FD"/>
        <Setter Property="BorderBrush" Value="#9ED2FD"/>
        <Setter Property="BorderThickness" Value="0"/>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ProgressBar}">

                    <Grid x:Name="TemplateRoot">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            Background="{TemplateBinding Background}"
            CornerRadius="10"/>

                        <Grid x:Name="Animation"
          Margin="{TemplateBinding BorderThickness}"
          ClipToBounds="True">
                            <!-- ✅ Prevent indicator from going outside -->

                            <!-- Track -->
                            <Rectangle x:Name="PART_Track" 
                   Fill="#9ED2FD"
                   RadiusX="3"
                   RadiusY="3"/>

                            <!-- Indicator -->
                            <Decorator x:Name="PART_Indicator">
                                <Grid x:Name="Foreground">
                                    <Rectangle x:Name="Indicator"
                           Width="70"
                           Fill="{TemplateBinding Foreground}"
                           RadiusX="4"
                           RadiusY="4">
                                        <Rectangle.RenderTransform>
                                            <TranslateTransform x:Name="IndicatorTransform" X="0" />
                                            <!-- start inside -->
                                        </Rectangle.RenderTransform>
                                    </Rectangle>
                                </Grid>
                            </Decorator>
                        </Grid>
                    </Grid>


                    <ControlTemplate.Triggers>
                        <Trigger Property="IsIndeterminate" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation
                                                Storyboard.TargetName="IndicatorTransform"
                                                Storyboard.TargetProperty="X"
                                                From="-200" To="200" Duration="0:0:2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BtnSignLoading" TargetType="Button">
        <Setter Property="Height" Value="40"/>

        <Setter Property="FontFamily" Value="DMSans"/>
        <Setter Property="Foreground" Value="{DynamicResource BtnSignLoadingFColor}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{DynamicResource BtnSignLoadingColor}"
                     
                     BorderBrush="Transparent"
                     BorderThickness="0.5"
                         
                     CornerRadius="20">
                        <ContentPresenter HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>

                        <Border.Resources>
                            <Style TargetType="Border">
                                <Setter Property="SnapsToDevicePixels" Value="True"/>
                            </Style>
                        </Border.Resources>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <!-- Hover -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="LightBlue"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Trigger>



        </Style.Triggers>

    </Style>



</ResourceDictionary>
